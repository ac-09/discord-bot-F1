BOT_PRESENCE={(str) Bot Presence when online}
DISCORD_TOKEN={(str) Discord Token}
LIVE={(str) Embed thumbnail when a F1 session is live}
TEXT_CHANNEL_IDS={(list) List of text channel IDs (from different guilds) where the bot will be operating in}
THUMBNAIL={(str) Default embed thumbnail}
THUMBNAIL_FAIL={(str) Embed thumbnail when embed fails to display schedule for next weekend.}

# Replace each field as appropriate (including the curly brackets) and rename file to ".env"
# Path to file in local directory (e.g. attachment://thumbnail.png) or a url to the image
